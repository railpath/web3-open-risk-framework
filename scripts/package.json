{
  "name": "risk-catalogue-validator",
  "version": "1.0.0",
  "description": "Validator for Risk/Indicator/Assessment/Measure markdown files with YAML blocks against JSON Schema.",
  "type": "module",
  "main": "dist/validate-markdown.js",
  "scripts": {
    "build": "tsc",
    "validate": "npm run build && node dist/validate-markdown.js",
    "validate-references": "npm run build && node dist/validate-references.js",
    "validate-all": "npm run validate && npm run validate-references",
    "dev": "ts-node --esm src/validate-markdown.ts",
    "dev-references": "ts-node --esm src/validate-references.ts"
  },
  "dependencies": {
    "ajv": "^8.12.0",
    "ajv-formats": "^3.0.1",
    "chalk": "^5.3.0",
    "js-yaml": "^4.1.0"
  },
  "devDependencies": {
    "@types/js-yaml": "^4.0.9",
    "@types/node": "^20.0.0",
    "ts-node": "^10.9.0",
    "typescript": "^5.0.0"
  }
}
